import { MultipleChoiceRiddle } from "../classes/MultipleChoiceRiddle.js";

let r10 = new MultipleChoiceRiddle(10, "Capitals", 2, "What is the capital of France?", "Paris", ["Berlin", "Rome", "Paris", "Madrid"]);

export default r10;
